export interface CSATPlan {
  title: string;
  subtitle: string;
  cost: string;
  currentCost: string;
  likes: string;
  open: boolean;
}

export interface Note {
  title: string;
  description: string;
  price: string;
  originalPrice: string;
  likes: number;
}